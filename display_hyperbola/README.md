# Use tkinter to build hyperbola display with on-screen parameter access

## Lightbulb implosions are saved in wavs_andrews_bay and wavs_orcasound_lab folders

## utilities.py has many needed utilities

## display_sources.py uses tkinter to plot hyperbolas and calculate mean square error
